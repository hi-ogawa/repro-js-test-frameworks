{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "playwright-install": "pnpx playwright install",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@playwright/test": "^1.36.0",
    "jsdom": "^22.1.0",
    "vitest": "^0.33.0"
  }
}
